---
title: TSK-TY-SHENG-001 SPEC 修订 patch-1
type: contract-patch
project: taiyi-school
parent: docs/luban-school/contracts/taiyi-architecture/2026-06-17_v1.4.0_TSK-TY-SHENG-001.md
version: 1.4.0-patch-1
date: 2026-06-17
status: ready
owner: 司南
---

# TSK-TY-SHENG-001 SPEC 修订 patch-1

## 一、来源

父契约：`docs/luban-school/contracts/taiyi-architecture/2026-06-17_v1.4.0_TSK-TY-SHENG-001.md`

鲁班执行时发现 SPEC-1、SPEC-4 验证命令设计缺陷，无法按原契约通过验证。本 patch 仅修订 SPEC 验证命令，不改动 STORY/DONE/DON'T 范围。

## 二、Delta 全表

| 章节 | 原内容 | 修订后 | 修订原因 |
|---|---|---|---|
| SPEC-1 | `grep -E "司衡\|siheng" skills/siheng/SKILL.md` 期望 PASS | `grep -E "司衡会自动唤醒你\|等待司衡调度\|司衡负责调度到下一步" skills/siheng/SKILL.md && echo "FAIL" \|\| echo "PASS"` | `skills/siheng/SKILL.md` 是司衡自身 SKILL，frontmatter/标题/核心准则必然包含"司衡""siheng"，原命令不可能通过。应检查强依赖描述而非名称本身。 |
| SPEC-4 | 全文件 `grep -E "司衡流水线内调度\|驱动唤醒\|自动唤醒"` 期望 PASS | 限定范围：九星能力卡片 + 自动续接机制 | SPEC-2 要求命中"当前环境不支持自动唤醒"，全文件 grep 必然冲突。应限定在"九星 SKILL 正文不依赖司衡"的关键区域。 |
| 其他 | 不变 | 不变 | 无缺陷 |

## 三、修订后的 SPEC 拆解

### SPEC-1：SKILL 正文不含对司衡自动调度的强依赖
- 验证命令：
  ```bash
  grep -E "司衡会自动唤醒你|等待司衡调度|司衡负责调度到下一步" skills/siheng/SKILL.md && echo "FAIL" || echo "PASS"
  ```
- 期望：PASS。
- 自主权：0%

### SPEC-2：含一次性降级规则（继承，不变）
- 验证命令：
  ```bash
  grep -E "一次性降级|不重试|请手动.*manual_invoke|当前环境不支持自动唤醒" skills/siheng/SKILL.md
  ```
- 期望：命中。
- 自主权：0%

### SPEC-3：司衡 SKILL 不写任何文件（继承，不变）
- 验证命令：
  ```bash
  grep -E "Write|Edit|mkdir|rm " skills/siheng/SKILL.md && echo "FAIL" || echo "PASS"
  ```
- 期望：PASS。
- 自主权：0%

### SPEC-4：九星能力卡片与自动续接机制不含司衡强依赖暗示（修订）
- 验证命令：
  ```bash
  awk '/^### 执行链星/,/^### 工具入口星/' skills/siheng/SKILL.md | grep -E "司衡流水线内调度|驱动唤醒|自动唤醒" && echo "FAIL" || echo "PASS"
  ```
- 期望：PASS。
- 自主权：0%

## 四、继承条款

以下条款完全继承父契约，本 patch 不做修改：

- STORY-1：司衡定位为"可选自动路由层"
- STORY-2：删除自动唤醒九星的强依赖
- STORY-3：增加手动召唤提示的降级路径
- DONE #1-#7 的验收目标
- DON'T 全部 7 条
- 依赖盘点
- 变更清单

## 五、依赖盘点

| # | 依赖类型 | 名称 | 本地可用？ | 接入方式 | 未就绪后果 |
|---|---|---|---|---|---|
| 1 | 父契约 | `docs/luban-school/contracts/taiyi-architecture/2026-06-17_v1.4.0_TSK-TY-SHENG-001.md` | ✅ | 已落盘 | 无修订对象 |
| 2 | 实现文件 | `skills/siheng/SKILL.md` | ✅ | 鲁班已修改 | 无验证对象 |
| 3 | 验证能力 | bash/grep/awk | ✅ | 本地 shell | 无法运行 SPEC 验证 |

## 六、契约确认

<HARD-GATE>
本 patch 确认后，鲁班继续按修订后的 SPEC 完成验证并交付 [榫卯]。
</HARD-GATE>

请确认：本 patch 是否可接受？确认后 @鲁班 按 `TSK-TY-SHENG-001` + patch-1 继续执行。
